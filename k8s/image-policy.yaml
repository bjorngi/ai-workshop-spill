apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: plasseringsspillet
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: plasseringsspillet
  policy:
    alphabetical:
      order: asc
